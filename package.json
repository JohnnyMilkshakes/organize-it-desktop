{
  "name": "learn-electron",
  "version": "1.0.0",
  "description": "following electron docs tutorial",
  "main": "main.js",
  "scripts": {
    "start": "npm run start-vite & wait-on http://localhost:5173 && electron .",
    "start-vite": "cd organize-it && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Johnny Milkshakes",
  "license": "MIT",
  "devDependencies": {
    "electron": "^32.1.2",
    "wait-on": "^8.0.1"
  }
}
